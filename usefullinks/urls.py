#!/usr/bin/env python
# -*- coding: utf-8 -*-
from django.conf.urls import patterns, url
from sjdl.usefullinks import views

urlpatterns = patterns('',
    #url(r'^$', views.Agrotourisme, name='index'),
    #url(r'^(?P<slug>.*)/$', views.fiche, name='fiche'),
)